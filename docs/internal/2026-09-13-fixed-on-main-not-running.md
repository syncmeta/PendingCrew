# 今天挨的这一整天，修的东西全在 main 上，一样都没在跑

基准提交：`b1c2243`。读数都来自 2026-09-13 00:2x 的同一轮查询。

## 三个读数

| 读数 | 值 |
| --- | --- |
| 正在跑的 `/Applications/PendingCrew.app` 二进制 mtime | 2026-09-12 00:11:08 |
| 那个进程的启动时刻 | 2026-09-12 00:11:10 |
| 装着的版本 | 0.1.34 (20707.55457) |

## 二进制出生之后，落在「账读不出来」这一族上的提交：16 笔

`git log --since='2026-09-12 00:11:08' -- Sources/Stores/{LocalWhiteboardStore,MultiProcessJSONStore,CaptainTodoSweepStore,LocalTodoStore,CockpitPlanStore,LocalWakeupStore}.swift`

其中三笔正好对着今天最疼的三件事：

- `d0fa33b` / `4a77e7a`（19:00–19:20）**白板读不动时把消息先存下来、恢复后自动补发**。
  所以我今天每一条发不出去的群消息，本该是「存下了，等下一个窗口补发」，
  而不是「一个字都没写、当作未送达」。
- `10f665f` / `6786d13`（11:29 / 20:39）**地板间隔**。
  所以「你停下来了，但这本账读不出来」本该退到地板间隔，
  而不是整夜按最短间隔问同一句话。
- `91e9d3c`（21:58）**账本的新文件改在数据根之外出生**，直接绕开这个周期性 EPERM。

旁证：数据根下**没有 `whiteboards/outbox/` 这个目录** —— 跑着的那个二进制
从来没有存过任何一条，因为它里面根本没有这段代码。

## 结论

今天这一整天的症状，**没有一条是未解问题**。它们全部已经修好、在 main 上、
被测试钉着。人和 agent 都在一个 19 小时前的二进制上挨这些已经修好的毛病。

唯一的出口是**装一个新构建**。而 0.1.35 卡在公证
（`No Keychain password item found for profile: pendingcrew-notary`，父 crew 人类 Todo #17）。
所以这不是工程问题了，是发版链路上的一个卡点。

**给未来的自己一条**：下次发现「这个毛病昨天不是修了吗」，第一件事不是重读代码，
是量正在跑的那个二进制的 mtime，拿它跟修复提交的时刻比。今天这一步晚了一整天。
