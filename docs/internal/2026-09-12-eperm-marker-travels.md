# EPERM 续：拒绝跟着**文件**走，而且发生在权限层之上

基准提交：`6786d13`（main）。本文只记 2026-09-12 21:2x–21:3x 这一次故障窗口内
亲手量到的读数，和它们各自的对照。**没有**用到 sudo，没有 GUI。

## 0. 结论

1. **标记跟着文件走，不跟着目录、也不跟着 inode 出生地。**
   `clonefile(2)` 按路径克隆、不 `open()` 源文件、产出**新 inode**。
   克隆一个坏文件到 `/private/tmp` 下 → **克隆体照样 EPERM**；
   在同一个目录里克隆一个好文件 → **克隆体可读**。
2. **拒绝发生在 VFS 权限判定之上。**
   坏文件的 `ATTR_CMN_USERACCESS` 返回 **6 = `R_OK|W_OK`** ——
   文件系统认为调用者有读权限，然后 `open()` 仍然 EPERM。
   所以这不是权限位/ACL/属主的问题，是一个 MACF/sandbox 策略钩子。

## 1. 量法（可复核）

全部用 ctypes 直呼 libc，脚本在当时的 scratchpad 里，没进仓库：

- `clonefile(src, dst, 0)` 与 `clonefile(src, dst, CLONE_NOFOLLOW)`
- `open(path, O_RDONLY)` 判可读
- `getattrlist` 取 `ATTR_CMN_USERACCESS` / `ATTR_CMN_CRTIME`
- `getxattr(..., XATTR_NOFOLLOW)` 取 `com.apple.provenance` 的**值**
- `acl_get_link_np` / `st_flags` / `mode` / `uid`

## 2. 四行主表（同一趟）

| 文件 | open | crtime | provenance 值 | USERACCESS |
|---|---|---|---|---|
| 坏·源（whiteboards/…approvals.json） | EPERM | 09-11 17:53 | `010200bec7230769fff17c` | 6 |
| 坏·源的克隆（落在 /private/tmp） | **EPERM** | 09-11 17:53 | 同上 | 6 |
| 好·刚建（同一个 /private/tmp 目录） | 可读 | 09-12 21:29 | **同上** | 6 |
| 好·刚建的克隆 | **可读** | 09-12 21:29 | 同上 | 6 |

第二行是本文的全部价值：**一个我自己建的、躺在 /private/tmp 下的文件，
每一项可见属性都和它旁边那个可读的邻居一样，就是打不开。**
它比数据目录好用得多 —— 可丢弃、归我所有、跟 TCC 保护位置完全无关。

## 3. 这一趟排除掉的（每条都有对照，不是"看起来不像"）

- **出生时间**：仓库里 09-01 出生的文件可读；把一个刚建的可读文件用
  `setattrlist(ATTR_CMN_CRTIME)` 改到 30 天前，**仍然可读**。
- **provenance 的值**：可读的和读不了的是**同一个值**
  （`010200bec7230769fff17c`）。此前"排除 provenance"比的是**有没有**这个
  xattr，那是个便宜的代理量；这次比的是值，结论同向但支撑硬了一档。
- **mode / uid / gid / st_flags / ACL**：三个文件逐项一致，ACL 全都没有。
- **`clonefile` 本身有害**：好文件的克隆可读，所以不是克隆这个动作把文件弄坏的。

## 4. 与既有结论的关系

- 早先记的"跟着 inode 的出生地走"**不成立**，本文第 1 条推翻它。
- 早先那条**唯一有判别力的对照**（同一个二进制：daemon 能读、claude 起的
  helper 不能）仍然站着。它说策略也认**读的那个进程**。
  两条合起来：策略同时看**读者**和**被读的文件**，
  像是"这个进程不许读带某种来源标记的文件"。
- 剩下的一步没变：故障窗口内 `sudo scripts/capture-eperm-fsusage.sh`，
  看是哪个策略在拒。那一步要人。

## 5. 边界

- 本文所有读数取自**一次**故障窗口（2026-09-12 21:2x 起，当时仍开着）。
  非故障时刻**没有**量过克隆行为，所以"好文件的克隆可读"这一行是
  同窗口内的横向对照，不是跨窗口对照。
- `ATTR_CMN_DOCUMENT_ID` / `ATTR_CMN_GEN_COUNT` 在这台机器上返回
  `EINVAL(22)`，**没有比过** —— 它们不在排除名单里。
- 第 4 条里"策略同时看读者和文件"是**推论**，不是观测：
  本文没有在同一时刻拿 daemon 去读那个 /private/tmp 下的克隆体。
  那个实验能做，且会直接证伪或坐实它。

---

## 6. 拿这个样本把「存下来再补发」跑了一次真的（2026-09-12 21:4x）

今天写的 spool（发不出去时先存着、白板重新读得动时补回）此前**只用
`chmod 000` 的替身验过**，那给的是 EACCES；真故障是 EPERM。用样本跑了一次
真的，做法是 `clonefile` 把样本搬到测试要的**确切路径**上（克隆不 open 源文件，
所以"打不开"这个性质搬得过去），然后走生产的 `appendSessionMessageReportingFailure`：

- 真 EPERM 下抛的是"读不出来"，错误里带的 errno 是 **EPERM(1)**，不是替身的 EACCES；
- 回执里明说"已经存进待发件箱"，`spooled == true`；
- 原件字节数一字未变；
- 把坏文件换成一份读得动的空表之后再发一条，白板上两条的顺序是
  **窗口里那条在前、窗口过去那条在后** —— 补回来的按发生时间排，没有倒挂。

**尺子验过会红**：把生产里那句 `spool.spool(...)` 变异成恒 `false`，
上面第 2 条和第 4 条**同时**红，且红在这两条自己的断言名上。

这个一次性测试**没有进仓库**：它依赖只存在于这台机器上的样本，
做成常驻测试会在 CI 上永远 skip，那就是一条从不发声的检查。
铸样本的手在 `scripts/diagnose-data-dir.sh` 的 ⑦，要复跑照着写一个就行。

**顺带一条**：真 EPERM 的 Foundation 文案是**繁体**的
（「無法打開檔案…因為你沒有權限檢視」），用户看到的就是这句。
errno 那段是我们自己拼的，稳定 —— 所以判据要挂在 errno 上，别挂在这句话上。

---

## 7. 第十条绕路：**它管用**（同夜，已落 main）

前九条全死（直接读 / `env -i` / `nohup` / `launchctl asuser` / `launchctl submit` /
硬链 / 四份历史备份 / 所有 open 模式 / MCP helper）。第十条成立：

> **在数据根之外建好文件，再 `rename` 进来。**

同一个故障窗口里量的三行：

| 在哪儿建的 | 建好时 | rename 进来之后 | 1.5 秒后 | 再原地改写一次 |
|---|---|---|---|---|
| `~/Library/Caches/` | 可读 | **可读** | 可读 | 成功，改完仍可读 |
| `/private/tmp/` | 可读 | **可读** | 可读 | 成功，改完仍可读 |
| `~/Library/Application Support/` 底下 | **读不了** | 读不了 | 读不了 | **失败 EPERM** |

### 7.1 拦的范围比「我们那个目录」宽

| 路径 | 新建的文件 |
|---|---|
| `AppSupport/PendingCrew/…` | 读不了 |
| `AppSupport/PendingCrew-<随便>.json`（兄弟，同前缀） | 读不了 |
| `AppSupport/PendingCrewX/`、`AppSupport/Pending/` | 读不了 |
| `AppSupport/ZZZ-unrelated/`、`AppSupport/` 根下 | **读不了** |
| `AppSupport/Code/`（别人家**已存在**的目录） | 可读 |
| `~/Library/`、`~/Library/Caches/`、`~/Documents/` | 可读 |

所以这不是「PendingCrew 这个名字被针对」。倒数第三行（一个跟我们毫无关系的新目录
也被拦）和倒数第二行（别人家已存在的目录不被拦）是这张表里最要紧的两格 ——
**没有它们，很容易把范围划在自己身上，而那正好是范围偏误最舒服的落点。**

### 7.2 一个能解释全部观测的模型（是模型，不是机制）

- 标记在**创建时**按创建位置打上，之后**跟着文件**走（`clonefile` 复制得过去）；
- 发作期间**带标记的一律 `open()` 被拒**，不发作时它们照常可读
  （数据目录里那些文件平时读得好好的，就是这一条）；
- 没标记的文件任何时候都读得动。

⚠️ **这是能解释观测的最简模型，不是查明的机制。** 具体哪个策略、标记存在哪儿，
仍然要发作当口的 `sudo` 抓取（人类 Todo #21）。

### 7.3 落地

`MultiProcessJSONStore.writeStaged(_:to:)`，落脚点 `~/Library/Caches/PendingCrew/staging`，
POSIX `rename` 进来（原子性不降级 —— `rename` 本来就是原子替换）。
落脚点建不了或跨卷退回 `.atomic`。

全仓 **16 处** `Data.write(options:.atomic)` 全部改走它 ——
Foundation 的 `.atomic` 把临时文件建在**目标目录里**，那正是每份账本
从出生起就带标记的原因。`NoRawAtomicWriteTests` 扫全仓钉住这条。

**只对改动之后新写的文件生效。** 已经带标记的老文件不会自己变好，
但每次整写都会把它换成一份没标记的，所以一个正常窗口过去之后账本就自愈了。

---

## 8. 第二个窗口的复核（2026-09-13 00:33）

§7 那三行读数全部来自**同一个**故障窗口。本仓有过「单窗口读数当通则」的教训，
所以在下一次发作（今天第五窗，00:24 起）里原样再跑一遍：

| | 结果 |
|---|---|
| 目录里既有的文件（agent-sessions.json 等） | 读不了 —— 窗口确实开着 |
| 直接在目录里新建 | 读不了 —— 与上一窗一致 |
| 外面建好再 `rename` 进来 | **可读，内容完整** |

所以「换个地方出生再挪进来」不是某一次的偶然，两个互相独立的窗口各成立一次。

⚠️ 仍然只是**两个**窗口。它是绕路不是根治，判据也还没查明（人类 Todo #21）。

**顺带一条读数**：这一窗里跑全量，跳过从 3 条变成 **6 条** —— 多出来的 3 条正是
发版闸门文档里记着的「第四种已知成因：数据目录读不出来时那 3 条也会跳」。
这条以前只是写着，今天第一次真的对上了。

## 8.1 一条自己栽的更正：「0 条积压」不等于「消费方在工作」

同一窗里我先判过「这次 daemon 没瞎」，依据是两条读数：
`*.crewcmd.json` **0 条**，以及 daemon 日志还在记 viewer 连入/断开。

**两条都不成立。** 0 条只说明那一刻没人入队（拿队列空当消费方在工作，
又一次用便宜的代理量替代那件事本身）；viewer 握手那几行不需要读数据目录，
所以它证明不了读得动。

真判据是**自己入一条再看它排不排得空**：`message_child_crew` 发一条，
`00:35:53` 起 16 分钟一动没动。所以这一窗 daemon 同样 open 不了既有文件，
**故障期间没有任何通道能把话送进群**。

留一条操作纪律：**故障期间发出去的探针消息，正文里别写会过期的结论。**
我那条写着「你们收到这条就说明这条路通」—— 它会在窗口结束后才送达，
那时这句话正好是反的。

## 9. 这条绕路留下的缺口：**老文件不会自己变好**

`writeStaged` 只管**以后新写的**文件。已经存在的那些是老方式出生的，
仍然带着标记，下一窗照样打不开。

- 频繁整写的（白板、Todo、驾驶舱账）**一次正常写入就换新了**，装上新版后很快自愈。
- **很少写的那几份最危险**：`local-crews.json`（通讯录 / crew 表）、
  机长模板、backend 表。它们可能几天才被写一次 —— 也就是说
  **人装了新版之后，下一窗里这几份仍然读不出来**，而他会以为已经修好了。

### 8.2 第三个窗口，绕路整条又成立一次（2026-09-13 07:05）

同一次发作（00:24 起，已连续 6 小时以上）。六步逐个记，**这次连「搬进去之后
再原地重写」也验了** —— 那一步是产品里真会发生的（`writeStaged` 之后的每一次
正常写入都走它）：

| 步 | 动作 | 结果 |
|---|---|---|
| ① | 在 `~/Library/Caches/PendingCrew/staging` 写一个新文件 | OK |
| ② | 在那儿读回来 | OK |
| ③ | `mv` 进 `whiteboards/` | OK |
| ④ | 搬进去之后再读 | **读得出来** |
| ⑤ | 原地重写（`> "$t"`） | OK |
| ⑥ | 重写之后再读 | **读得出来** |

**同一趟里还撞到一条更锋利的**：用 `mktemp` 直接在 `whiteboards/` 里建一个文件，
`mktemp` 自己成功（目录项建出来了、`ls` 看得见、0 字节），但**紧接着的
`> "$c"` 当场 EPERM** —— 也就是说**它连自己刚生出来的那个文件都重开不了**。

这把第 8 节那条「直接在目录里新建 → 读不了」收紧了一档：**不是「建好之后过一会儿
被标记」，是出生的那一刻就带着**。同一秒内，同一个进程，自己建的，打不开。

顺带复核了一次 flags 和 ACL（`ls -lO` / `ls -le`）：被拒文件两样都空。
**这不是新发现** —— 第 3 节已经用「三个文件逐项一致」排除过，而且那个对照比
我这次的强（我只看了一个文件）。记在这儿只是说它在第三个窗口里没有变。

### 8.3 故障期间备份数据目录：只有 `clonefile(2)` 过得去

这条是被规矩逼出来的。仓库里那条「**换掉正在跑的 app 之前先冷备份数据目录**」
（2026-08-11 白板被清光那次立的）在发作期间**执行不了** —— `cp` / `ditto` /
`rsync` / `tar` 全都要 `open()` 源文件，而 open 正是被拒的那个。
`cp -c` 也不行：它在决定用不用克隆之前就先 open 了（实测同一句 EPERM）。

`clonefile(2)` 过得去，因为它**按路径克隆、从不打开源**。而且它能整棵目录一次调用。
2026-09-13 07:09 实测：整个数据根一次克隆成功，两边 `find -type f` 都是 **4602** 个。

已经做成 `scripts/clone-data-root.sh`（`python3` + `ctypes` 调 libSystem，
不用编译器）。绿、红都当场证过：目标已存在 → 退出码 2；跨卷 → `errno=18 EXDEV`、
退出码 1、目标没被创建出来。

⚠️ **克隆出来的文件此刻同样读不出来**（标记跟着克隆走，本文第 1 条）。
这是预期，不是备份失败：字节在里面，等这一窗过去就读得动。
备份的意义是「换 app 之前把现状钉住」，不是「现在就能翻」。

**这一步能给的最强判据只是数文件个数** —— 逐字节比对要 open()，做不了。
（自己踩的一脚：第一次量跨卷那条的退出码是**经管道**读的 `$?`，读到的是
`head` 的 0，差点记成「红了但没拦住」。重量一次是 1。）

### 8.4 这一窗到 08:44 已经连续 8 小时 20 分（2026-09-13）

第 5 次发作 00:24 起，到 08:44 一直没开过窗。一个 6 小时到期的守候进程刚刚
报「等满 6 小时仍未恢复」。

**所以「它会自愈」这句话要带上尺度**：仓库里最早那个样本是 44 分钟，后来改口说
「44 分钟只是一个样本」；现在有了一个**8 小时以上**的样本。
差别不是学术的 —— 44 分钟可以等，8 小时不能等：
这段时间里 agent 这侧发不出群消息、核不了账、读不了任何已有文件，
而**人只会看到它安静**。

对人的口径也要跟着改：**别再说「等它自己好」**。要么跑
`sudo scripts/capture-eperm-fsusage.sh` 去查是谁在拒，要么先把手上能做的做完
（发版、写文档这些不碰数据根的活照常）。

### 9.1 想彻底关掉要做一次性迁移，但今晚**故意没做**

做法本身不难：启动时把数据根里每个文件按字节读出来、用 `writeStaged` 原样写回去、
回读比对，比不上就原样留着不动。幂等，也不改内容。

**没做的理由是风险面**：这是对用户全部账本做一次批量重写，而今晚
① 数据目录正处在读不出来的状态，做不了端到端验证；
② app 装着的还是 0.1.34，改完也验不了真实启动路径。
一个只在单测里验过的批量重写，第一次真跑是在别人的数据上 —— 那不该在
发版当夜、无人看着的时候发生。

**所以这条留给人拍**：要不要做这次迁移、什么时候做。等账本读得回来会挂成待办。
在那之前，**「装上 0.1.36 就再也不会瞎」这句话是不准确的** ——
准确的说法是「新写的不会再瞎，老文件要等它被写过一次」。
