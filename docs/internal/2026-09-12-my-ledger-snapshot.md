# 本 crew 两本账的快照（2026-09-12 11:26:23 那个窗口）

基准提交：`1d9b18a`（搬出来那一刻的 main HEAD）。
原件位置：`~/Library/Application Support/PendingCrew/whiteboards/local-21533801-….{todos,human-todos}.json`，
原样复制在 `2026-09-12-ledger-snapshot/` 里（没有改一个字节）。

## 为什么有这份东西

昨天那个窗口里我读了父 crew 的账，**忘了读自己这本** —— 窗口关上之后
「我自己还剩几条」又变成答不上来的问题，而提醒每隔最短间隔就来问一次。
这次窗口一开，第一件事是把账搬出那棵树，别的都往后排。

## 读数

**Agent Todo（人类派给我们的）：共 3 条，全部 `completed`，0 条未完成。**

| # | 状态 | 事 |
| --- | --- | --- |
| 1 | completed | todo 测试 |
| 2 | completed | 输入框 placeholder 改成「发群消息/新建 Todo」，输中文/候选态要消失 |
| 3 | completed | 终端没开时，底下输入框换成跟群聊一样的样式 |

**人类 Todo（我们请人拍板的）：共 2 条，都是 `pending`。**

| # | 事 |
| --- | --- |
| 1 | #104 折叠：默认折到什么程度（A 保守只折代码块/表格/终端输出 ／ B 激进） |
| 2 | #104 折叠：几行以上才折（>8 行 ／ >4 行） |

这两条不卡我干活，但从提出到现在没被回过。

**父 crew 的人类 Todo：17 条，12 条 pending**，其中 #16 仍然 pending ——
所以 #142/#143 严格性那一半继续挂着是对的，不是被我忘了。

## 边界

- 这是**一个时刻**的快照，不是活账；引用它之前先看看窗口开没开。
- 驾驶舱那本（`.plan.json`）这次也搬出来了，但没在这里展开。
