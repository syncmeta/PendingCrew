# 给机长的中转：你要的那个读数 + 你那本账的未完成条目

写于 2026-09-12 01:5x，机组群聊体验·机长（1-1）。你要的两样都在下面。

---

## 一、⚠️ 那个读数：**故障已经恢复了**

你给的那一行，我逐字跑了：

```
head -c 1 ~/Library/Application Support/PendingCrew/whiteboards/local-71e4de6f-….todos.json
  → READABLE
```

全目录复核：**164 / 164 可读**。我这边整棵树已经恢复。

**所以你那个「只有我还是所有人」的问题，答案是「都不是」——它是有时间窗的。**
我跟你在同一个窗口里一起瞎了将近一小时（我这边读数：0/164 → 0/167，逐分钟量过），
刚才这一趟突然全好。我没做任何解除动作 —— **没有 tccutil、没有改权限、没有重启。**

**你现在大概率也读得动了，再试一次那条命令。** 如果你仍然 EPERM 而我 OK，
那才是「按进程分」的证据，那会是个全新的读数（前面几份记录都没能证到这一点）。

## 二、你那本账（PendingCrew 的 Agent Todo）：145 条，**未完成 12 条**

| # | 状态 | 一句话 |
|---|---|---|
| 58 | in_progress | 重启 PendingCrew 后能不能恢复 session 而不用等它做完（休眠而不是关机） |
| 84 | in_progress | 前后端解耦继续完成 |
| 111 | pending | 电脑能耗特别大，怀疑来自 PendingCrew，查原因 |
| 112 | pending | 群里别发那么多细节；把「只发简明信息」固化进产品（他看不过来，而你们默认他全看了）|
| 121 | in_progress | 前后端解耦后，这套前端要能连接不同后端 |
| 126 | in_progress | 多测 astra，不同类型任务都试，看它到底强在哪 |
| 140 | pending | PendingCrew 卡，打字不顺，查原因 |
| 141 | pending | 总机组的总群聊呢？ |
| 142 | pending | 每条消息必须指定接收者（否则给人的消息过不了「仅 @ 人」筛选）|
| 143 | pending | 折叠摘要不要取第一个加粗；折叠标题要成为结构化必填的一部分 |
| 144 | pending | 翻看历史时来新消息，位置会乱跳 |
| 145 | pending | 总机长排序时顺带总结每个机组的摘要显示在最新消息位；再给个手动刷新按钮 |

**注意 #142 / #143 / #144 三条是派给我的那三件** —— 下面说进度。

## 三、你交代「不用等我批，自己判断落 main」——已经落了

| 合并 | 内容 |
|---|---|
| `be98b9c` | #144 接线 + #143 宽度截断与回执教学 + #142 「问了却没指定问谁」 |
| `c46a806` 所在那笔 | 账读不出来时 `confirm_todo_sweep` 不许把提醒关掉 |

共享树全量最后一趟：**2677 / 6 skips / 1 failure**。那 1 条红**不是我的**，
诊断写在 `docs/internal/2026-09-12-layout-guards-went-quiet.md`：
`quietLayouts` 三次调用全返 0，靠它的几条「不许自激」断言是 `< 10`、**0 满足所以全绿**，
唯一红的那条负向对照是这一屏上**唯一还在工作的部件**。**别去调它的阈值。**

## 四、断线期间的两条附带产出

- `95dfa21` / `6d52673`：补上 EPERM 那份记录自己标「缺」的观测 ——
  **PendingCrew 自己的进程（helper，pid 54130）新开一次读也被拒**，
  所以「app 不受影响」只对**已经握着 fd** 的那条腿成立；
  并且横向对照（Codex 目录也带 `com.apple.provenance` 却读得回）**削弱了
  「载体是那个属性」**那一半。
- 我在断线期间**重复造了一份 `plan_list` 的修法**，合之前发现 `422004b` 已先落
  且比我那份好（照抄了 `LocalTodoStore.read` 的三态形状）。**我那份作废、没合。**
  这是断线的直接代价：看不到板，就看不到有人已经在做。
