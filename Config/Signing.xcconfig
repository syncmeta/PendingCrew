// PendingCrew 的签名配置
//
// **这份文件必须待在子目录里，不能挪回仓库根目录。** XcodeGen 给「位于项目
// 根目录的 config file」生成的是 `TEMP_<随机 UUID>` 引用 —— 每跑一次
// `xcodegen` 那几个 id 就变一次，pbxproj 于是永远是脏的，CI 的漂移检查会
// 天天红。放进子目录它才走正常的确定性 id 生成。2026-08-20 实测。 —— 这份是**仓库默认值**，任何人 clone 下来都能直接编。
//
// 默认走 ad-hoc（「Sign to Run Locally」）：不需要 Apple 开发者账号、不需要
// 任何证书，`xcodebuild` 和 Xcode 都能把 app 编出来跑起来。开源贡献者不用改
// 任何被 git 跟踪的文件。
//
// 但 ad-hoc 有一个**真实的代价**，别以为它只是「没签名而已」：
//   `KeychainStore`（云端 crew 的 device-grant token，默认钥匙串、
//   kSecAttrService=com.pendingname.pendingcrew）的 ACL 绑当前签名身份。
//   ad-hoc 每次重新构建身份就变 → 系统认不出重建的 app → 反复弹「存取钥匙串」
//   授权框，或直接 -34018 存不住 → 登录态丢失。
// 只跑本机 crew（起 claude / codex 子进程那条主路径）完全不受影响；要调云端
// 登录相关的东西，就得用一个**稳定**的签名身份。
//
// 用稳定身份的做法：把 Config/Local.xcconfig.example 复制成 Config/Local.xcconfig
// （已在 .gitignore 里，不会被提交），填你自己的 Apple Team ID。下面这行
// 可选 include 会把它的值盖上来。
//
// 发版**会**读这份文件（那份干净快照里没有 Local.xcconfig 来盖它），所以
// scripts/release/build-macos-update.sh 必须在 xcodebuild 命令行上把
// CODE_SIGN_STYLE / DEVELOPMENT_TEAM / CODE_SIGN_IDENTITY / CODE_SIGN_ENTITLEMENTS
// 四个键**全部**补齐 —— 命令行优先级最高，盖得住下面这些默认值（含
// `[sdk=macosx*]` 条件赋值，实测过）。
//
// 这里原来写的是「发版不读这份文件……不会被这里的默认值影响」，那句是错的：
// 当时补了三个键、漏了 CODE_SIGN_IDENTITY，于是下面的 ad-hoc `-` 直接落到发版
// 构建上，2026-08-24 出 0.1.14 时 ARCHIVE 当场失败
// （"entitlements that require signing with a development certificate"）。
// 往这里加新键的话，记得同步看一眼发版脚本有没有把它盖住。

CODE_SIGN_STYLE = Manual
CODE_SIGN_IDENTITY = -
DEVELOPMENT_TEAM =
CODE_SIGN_IDENTITY[sdk=iphonesimulator*] = -
CODE_SIGN_IDENTITY[sdk=macosx*] = -

// entitlements 也必须跟着走，不能只换签名身份。
// `Resources/PendingCrew.entitlements` 里的 keychain 组写成
// `$(AppIdentifierPrefix)com.pendingname.pendingcrew` —— 那个变量**只能由
// provisioning profile 展开**。没 team 就没 profile，Xcode 会直接报
// 「requires a provisioning profile」**编译失败**（不是警告）。所以仓库默认
// 构建不带 entitlements；要它的人在 Config/Local.xcconfig 里连同 team 一起打开。
// 少这个 entitlement 只影响云端登录用的钥匙串（device-grant token / 家族
// 共享凭据）——本机 crew 那条主路径一次都不碰钥匙串。
CODE_SIGN_ENTITLEMENTS =

#include? "Config/Local.xcconfig"
