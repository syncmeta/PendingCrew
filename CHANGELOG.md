# 更新日志

每个版本这里写一段，`## <版本号>` 开头。发版脚本从这里取文案，同时喂给
GitHub Release 正文和 app 内的更新弹窗 —— **这是更新日志的唯一出处**，
别在别处另写一份。

写给用的人看，不是给读代码的人看：一条一句话，说清这版对他有什么不同。
提交标题不要原样搬进来。

## 0.1.19

- Codex 从较长的旧对话恢复时，不再因一次返回整段历史而被 25 秒启动检查误判成“半死”；机长和普通 Codex session 都可以正常续上原对话

## 0.1.18

- 新建 session 的窗口多了「Session 类型」：可以直接建一个机长，它以机长的世界观和工具权限启动。选了之后当前机长会停止，由这个新 session 接任，用的是一段全新对话
- 机长把机长身份交出去这件事现在是事务性的：交接中途出岔子会自动把原来那个恢复成机长，不会留下一个没有机长的机组
- 侧栏能看到下属机组的人类 Todo 了。以前只有本机组等你拍板的条目会让那一行亮起来，子机组里的看不见；现在它会顶到父机组那一行，悬停能看清是本机组几条、下属几条
- 侧栏的状态指示收敛成三种：机组正在工作、机组有运行错误、有人类 Todo 等你拍板（黄色呼吸）。旧的那套 attention 文案清掉了
- 发版链路和 CI 做了一轮加固，你感觉不到：构建快照和 tag 现在钉同一个提交，main 每次改动都会真的被编译和跑测试

## 0.1.17

- 机组成员的 agent session 现在可以被重新指定为机长：在成员头像上右键选择「设为机长」，旧机长会停止，被选中的原对话会续接成新机长；即使交接碰上安装或重启，新版启动后也会自动续完
- 修：群聊里回复某一条消息，现在会自动 @ 上那条的发送者，对方才会被叫醒。在这之前，回复是回了、却谁也没点到，而发送回执照样显示「已发到」—— 从外面完全看不出来，所以这一版装上之前，这个坑一直在静默生效

## 0.1.16

这一版的主角是人类 Todo 面板。

- Todo 面板现在分两本账：你派给我们的、和我们请你拍板的，两个药丸各管各的，不再混在一起
- 「我们问你的」那本你可以直接在上面回应 —— 回应会把提问的那个成员叫醒，它接着往下干
- 每条还多了一颗「不办」：按下去黄点就灭。不用为了消掉一个提醒，去假装办一件你本来就不打算办的事
- 启动不再有登录页，直接进主界面；跨端遥控那一整层也一起端掉了，代码少了六千多行
- 侧栏按最新动静排序；在群上右键可以把它藏起来，藏了多少在底部「已隐藏的群 (N)」里一眼看得见，随时取得回来
- 群聊里打 @ 时的候选列表有了高度上限、可以滚；「只看 @ 我的」那个筛选钮换成了群名旁边的文字药丸「仅@你」
- 「加载更早」不再闪一下 —— 你正在读的那条本来就在原地，不再先被移走再移回来
- 重启之后，session 认得原来那段对话，接着聊
- 终端内部做了一轮改造，你应该感觉不到

先说清一条边界，免得按旧印象撞上：重启之后 session 认得原来的对话，这件成立了；但关掉 app、或者装更新的时候 session 不停，这件还没有。⌘Q 或者装更新，正在跑的 session 仍然会断。这两件用起来像是同一件事，其实是两条线，这一版只修好了前一半。

## 0.1.15

- 侧栏的机组列表按**最新动静**排序，不再按创建时间 —— 刚说过话的排在上面，父机组跟着最活跃的那个子机组走
- 启动不再有登录页，直接进主界面 —— 云端登录整层已经删掉，这个 app 不连任何后端
- 驾驶舱的任务段现在也能看到机长自己排的活，不只是从别处读来的

## 0.1.14

- 群聊加了「只看 @ 我的」筛选器：工具条上的 @ 钮点亮，就只剩点了你名的消息和你自己发的
- 「加载更早的消息」不再把你甩走 —— 新消息插在上面，你正读的那条留在原地
- 修：一条只 @ 了人类的消息，会从所有 agent 队友眼前隐身。@ 人类现在只是个标记，不再把队友挡在外面

## 0.1.13

首个公开版本。

- 建机组、起 Claude Code / Codex session，可以共用一个目录，也可以各自独立的 git worktree
- 群聊白板：成员之间互相看得见，能 @ 点名、能交接、需要你拍板时会把问题发到群里
- 每个成员背后是一个真终端，你随时可以接管键盘
- 机长自己派活、起新成员、按需要拆出子机组
- 驾驶舱、To Do 面板、审批与定时唤醒
- 自动更新
